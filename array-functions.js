


const numbers = [1,2,3]

const doubleNumArray = numbers.map((num)=>{    // map function on each item in array
    return num*2;                              // and created new array
});

console.log(numbers);
console.log(doubleNumArray);